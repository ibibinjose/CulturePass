# Cloud Functions / Firebase — set via Firebase Console → Functions → Configuration
# or `firebase functions:secrets:set` (recommended for production). Do not commit real values.

# Stripe (server only)
# Preferred server key. Use a restricted key (`rk_...`) with least-privilege permissions for this Functions API.
STRIPE_API_KEY=
# Legacy fallback. Prefer STRIPE_API_KEY for new Stripe accounts.
STRIPE_SECRET_KEY=
# Optional server echo for mobile PaymentIntent responses. Keep this in sync with EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY.
STRIPE_PUBLISHABLE_KEY=
# Webhook signing secret — endpoint must receive Connect event: account.updated (plus existing checkout/subscription events)
STRIPE_WEBHOOK_SECRET=
# CulturePass+ subscription — default market (AU). Per-market overrides use __{MARKET} suffix.
STRIPE_PRICE_MONTHLY_ID=
STRIPE_PRICE_YEARLY_ID=
# Optional multi-market Stripe Price IDs (US, GB, NZ, AE)
# STRIPE_PRICE_MONTHLY_ID__US=
# STRIPE_PRICE_YEARLY_ID__US=
# STRIPE_PRICE_MONTHLY_ID__GB=
# STRIPE_PRICE_YEARLY_ID__GB=
# STRIPE_PRICE_MONTHLY_ID__NZ=
# STRIPE_PRICE_YEARLY_ID__NZ=
# STRIPE_PRICE_MONTHLY_ID__AE=
# STRIPE_PRICE_YEARLY_ID__AE=

# Optional — CultureShop first-time Plus discount (create a 50% coupon in Stripe Dashboard)
STRIPE_COUPON_FIRST_PREMIUM_HALF_OFF=

# Optional — Firebase users/{uid}.approvedMarketplacePublisher lets organisers publish CultureShop deals
# Stripe Connect — marketplace application fee (basis points). Default in code is 1000 (= 10%).
STRIPE_CONNECT_PLATFORM_FEE_BPS=1000

# Optional Firestore pricing_plans overrides — seed via: npx tsx scripts/seed-pricing-plans.ts
# GET /api/pricing/plans?country=Australia returns catalog + live Stripe amounts
FIREBASE_STORAGE_BUCKET=
ROLLOUT_PHASE=internal

# Apple Wallet pass signing (if using pass generation for business cards + tickets)
# All of the below (plus passphrase) are required for Apple .pkpass generation.
# Use `firebase functions:config:set` or secrets for production; never commit values.
APPLE_WWDR_CERT_PEM=
APPLE_PASS_SIGNER_CERT_PEM=
APPLE_PASS_SIGNER_KEY_PEM=
APPLE_PASS_SIGNER_KEY_PASSPHRASE=
APPLE_PASS_TYPE_IDENTIFIER=
APPLE_TEAM_IDENTIFIER=
WALLET_LINK_SIGNING_SECRET=

# Google Wallet (for business cards + tickets). Service account private key can be multi-line or \n escaped.
GOOGLE_WALLET_ISSUER_ID=
GOOGLE_WALLET_SERVICE_ACCOUNT_EMAIL=
GOOGLE_WALLET_PRIVATE_KEY=
# For member business-card "save to wallet" (generic class). Tickets use a separate or same class.
GOOGLE_WALLET_GENERIC_CLASS_ID=
# Optional override for ticket class id (defaults to culturepass_event_ticket under issuer)
# GOOGLE_WALLET_TICKET_CLASS_ID=

# Eventbrite Australia sync — private token from https://www.eventbrite.com/platform/
# Used by daily sync into the Australian Event Finder community (tickets stay on Eventbrite).
EVENTBRITE_PRIVATE_TOKEN=

# Optional: override origin used for wallet deep links / .pkpass download (defaults to culturepass.app or request host)
# PUBLIC_APP_ORIGIN=https://culturepass.app
# WALLET_LINKS_PUBLIC_ORIGIN=https://culturepass.app
# APPLE_PASS_WEBSERVICE_URL=https://culturepass.app/api/wallet/apple/v1

# Local emulator seed only (`npm run seed:emulator:cap` from functions/, or `npm run emulator:seed:cap` from repo root).
# Script forces FIRESTORE_EMULATOR_HOST / FIREBASE_AUTH_EMULATOR_HOST defaults — do not use in production deploys.
# SEED_TEST_EMAIL=cap-emulator@test.local
# SEED_TEST_PASSWORD=
# SEED_PROJECT_ID=culturepass-4f264
