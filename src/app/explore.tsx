import { ExploreTabScreen } from '@/modules/explore/screens/ExploreTabScreen';

/** /explore — canonical CultureX surface (no longer a tab). */
export default function ExploreRoute() {
  return <ExploreTabScreen />;
}
