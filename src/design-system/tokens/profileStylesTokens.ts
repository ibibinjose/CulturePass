/** Shared profile tab StyleSheet palette (FIXES-001 P21). */

import { BorderTokens } from './theme';

export const PROFILE_STYLES = {
  onHero: BorderTokens.white,
  shadow: BorderTokens.black,
  surface: BorderTokens.white,
} as const;